apiVersion: batch/v1
kind: CronJob
metadata:
  name: presto-to-hcd
  namespace: affiliate-junction
  labels:
    app.kubernetes.io/name: affiliate-junction
    app.kubernetes.io/component: presto-to-hcd
spec:
  schedule: "*/1 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      backoffLimit: 2
      template:
        metadata:
          labels:
            app.kubernetes.io/name: affiliate-junction
            app.kubernetes.io/component: presto-to-hcd
        spec:
          serviceAccountName: affiliate-junction-sa
          restartPolicy: OnFailure
          containers:
            - name: presto-to-hcd
              image: affiliate-junction:latest
              imagePullPolicy: Always
              command: ["python", "presto_to_hcd.py"]
              env:
                - name: CONFIG_PATH
                  value: /config/config.yaml
                - name: HCD_USERNAME
                  valueFrom:
                    secretKeyRef:
                      name: affiliate-junction-secrets
                      key: HCD_USERNAME
                - name: HCD_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: affiliate-junction-secrets
                      key: HCD_PASSWORD
                - name: PRESTO_USERNAME
                  valueFrom:
                    secretKeyRef:
                      name: affiliate-junction-secrets
                      key: PRESTO_USERNAME
                - name: PRESTO_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: affiliate-junction-secrets
                      key: PRESTO_PASSWORD
              volumeMounts:
                - name: config
                  mountPath: /config
                  readOnly: true
              resources:
                requests:
                  memory: "512Mi"
                  cpu: "250m"
                limits:
                  memory: "1Gi"
                  cpu: "500m"
          volumes:
            - name: config
              configMap:
                name: affiliate-junction-config

# Made with Bob
